"""Shared auth contract for CCP4i2 and consumers.

This package is the Python half of a bilingual auth library; the TypeScript
half is published as ``@ccp4/ccp4i2-auth``. Both halves implement the same
canonical bearer-token format and 401 response shape.

See ``apps/compounds/docs/CCP4I2_FACING_PROPOSAL_REPO_SPLIT.md`` (in the
ccp4i2 repo, during the monorepo phase) for the full design and the README
for current status.
"""

__version__ = "0.0.0.dev0"
