# CCP4i2 Docker Environment Configuration
# Copy this file to .env and modify as needed

# =============================================================================
# CCP4 Data Path (for shell container and Azure Files)
# =============================================================================
# Root directory for CCP4 data - mounted read-write in shell container
# This is where you'll install/build CCP4 for Docker deployment
#
# For local development with existing CCP4:
# CCP4_DATA_PATH=./data/ccp4data
#
# For Azure Files mount (after mounting the share to your host):
# CCP4_DATA_PATH=/Volumes/ccp4data
# or on Linux:
# CCP4_DATA_PATH=/mnt/azure/ccp4data
CCP4_DATA_PATH=./data/ccp4data

# =============================================================================
# CCP4 Installation Path (for server container)
# =============================================================================
# Path to your local CCP4 installation (will be mounted read-only by server)
# This should point to the ccp4-9 directory inside CCP4_DATA_PATH once built
CCP4_PATH=/opt/ccp4-9
# Or on macOS:
# CCP4_PATH=/Applications/ccp4-9
# Or using the data path:
# CCP4_PATH=./data/ccp4data/ccp4-9

# =============================================================================
# Projects Directory
# =============================================================================
# Where CCP4i2 projects and job files will be stored
PROJECTS_PATH=./data/projects

# =============================================================================
# Security
# =============================================================================
# Secret key for Django (CHANGE THIS IN PRODUCTION)
SECRET_KEY=dev-secret-key-change-in-production

# =============================================================================
# Server Configuration
# =============================================================================
SERVER_PORT=8000
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1
USE_GUNICORN=false

# =============================================================================
# Client Configuration
# =============================================================================
CLIENT_PORT=3000
API_URL=http://localhost:8000
NODE_ENV=development

# =============================================================================
# CORS Configuration
# =============================================================================
CORS_ALLOWED_ORIGINS=http://localhost:3000

# =============================================================================
# Database Configuration (Optional - defaults to SQLite)
# =============================================================================
# For SQLite (default):
# DATABASE_URL=sqlite:////mnt/ccp4data/ccp4i2-projects/ccp4i2.sqlite

# For PostgreSQL (requires --profile postgres):
# DATABASE_URL=postgresql://ccp4i2:ccp4i2@db:5432/ccp4i2
# DB_USER=ccp4i2
# DB_PASSWORD=ccp4i2
# DB_NAME=ccp4i2
# DB_PORT=5432

# =============================================================================
# Azure Configuration (for azcopy-files.sh and cloud deployment)
# =============================================================================
# Storage account name (required for azcopy)
# AZURE_STORAGE_ACCOUNT=mystorageaccount

# Resource group containing the storage account
# AZURE_RESOURCE_GROUP=myresourcegroup

# Azure Files share name (default: ccp4data)
# AZURE_FILE_SHARE=ccp4data

# Azure tenant ID (optional - for multi-tenant accounts)
# AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# SAS token expiry in hours (default: 8)
# AZURE_SAS_EXPIRY_HOURS=8
