# Office lock files (created while a .pptx is open) — never commit
~$*

# Exported artifacts — the .md is the source of truth; regenerate as needed.
# Remove these lines if you want to track a shareable export.
*.pdf
*.pptx
