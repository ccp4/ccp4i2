# Deck images — CCP4i2 @ Cosener's 2026

Screenshots captured on a **Windows** test VM (2026-07-03), running the
`3.1.0a1` alpha. Save each grab with the exact filename below; the deck
(`../ccp4i2-coseners-2026.md`) embeds them by these names.

| File | What it shows | Deck slide |
|---|---|---|
| `mtz-reciprocal-previewer.png` | MTZ reciprocal-space previewer (gamma_xe_mosflm.mtz, P222, HK plane, coloured by intensity) | "And it's actually richer to use" |
| `report-servalcat-chartjs.png` | Servalcat REPORT — refinement table + Chart.js Correlations / RMS-deviation plots | "Demo — and real reports" |
| `molrep-self-rotation-svg.png` | MolRep Self-Rotation Function (PostScript→SVG) polar plots | "We even modernised the legacy outputs" |
| `moorhen-windows.png` | **Moorhen** 3D — model + electron density (2Fo-Fc/Fo-Fc mesh) on Windows | "Same codebase — every platform" (hero) |

Optional extra (not referenced by the deck, but a strong "Windows desktop"
context shot — Recycle Bin / taskbar visible):

| File | What it shows |
|---|---|
| `windows-servalcat-interface.png` | CCP4i2 Servalcat task interface on the Windows desktop |

All are PNG. Keep them reasonably high-res (maximise the RDP window before the
Mac-side `Cmd+Shift+4` grab).
