import { LinearProgress, Paper, Typography } from "@mui/material";
import { CCP4i2TaskInterfaceProps } from "../task-container";
import { CCP4i2TaskElement } from "../../task-elements/task-element";
import { CCP4i2Tab, CCP4i2Tabs } from "../../task-elements/tabs";
import { CCP4i2ContainerElement } from "../../task-elements/ccontainer";
import { useJob } from "../../../../utils";
import { useMemo } from "react";

const TaskInterface: React.FC<CCP4i2TaskInterfaceProps> = (props) => {
  const { useTaskItem, container } = useJob(props.job.id);
  const { value: CONFORMERSFROM } = useTaskItem("CONFORMERSFROM");
  const { value: MOLSMILESORSKETCH } = useTaskItem("MOLSMILESORSKETCH");
  
  if (!container) return <LinearProgress />;
  
  return (
    <Paper>
      <CCP4i2Tabs>
        <CCP4i2Tab key="inputData" label="Input data">
          <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
            
          </Typography>
          <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
            
          </Typography>
          <Typography variant="subtitle1" sx={{ fontWeight: "bold", mt: 2, mb: 1 }}>
            Start point
          </Typography>
          <CCP4i2ContainerElement
            {...props}
            itemName=""
            qualifiers={{ initiallyOpen: true }}
            containerHint="BlockLevel"
          >
            <CCP4i2TaskElement itemName="MOLSMILESORSKETCH" {...props} qualifiers={{ guiLabel: "Start with molecular structure from" }} />
            <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
              Will launch Lidia to sketch molecule. Click Apply and Close in Lidia when sketch is ready.
            </Typography>
            <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
              Optionally can provide a starting monomer for the Lidia sketch:
            </Typography>
            {(MOLSMILESORSKETCH === "SKETCH" || MOLSMILESORSKETCH === "MOL") && (
              <CCP4i2TaskElement itemName="MOLIN" {...props} />
            )}
            {(MOLSMILESORSKETCH === "SMILES") && (
              <CCP4i2TaskElement itemName="SMILESIN" {...props} qualifiers={{ guiLabel: "SMILES string" }} />
            )}
          </CCP4i2ContainerElement>
          <Typography variant="subtitle1" sx={{ fontWeight: "bold", mt: 2, mb: 1 }}>
            Output monomer
          </Typography>
          <CCP4i2ContainerElement
            {...props}
            itemName=""
            qualifiers={{ initiallyOpen: true }}
            containerHint="BlockLevel"
          >
            <CCP4i2TaskElement itemName="TLC" {...props} qualifiers={{ guiLabel: "Three letter code for output monomer" }} />
          </CCP4i2ContainerElement>
        </CCP4i2Tab>
        <CCP4i2Tab key="folder_1" label="Advanced">
          <Typography variant="body2" color="text.secondary" sx={{ mb: 1 }}>
            Geometry restraints *always* taken from ACEDRG
          </Typography>
          <CCP4i2TaskElement itemName="CONFORMERSFROM" {...props} qualifiers={{ guiLabel: "Output conformers generated by" }} />
          {(CONFORMERSFROM === "RDKIT") && (
            <>
              <CCP4i2TaskElement itemName="NRANDOM" {...props} qualifiers={{ guiLabel: "Number of random RDKit start structures" }} />
            </>
          )}
        </CCP4i2Tab>
      </CCP4i2Tabs>
    </Paper>
  );
};

export default TaskInterface;