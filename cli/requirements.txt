# Requirements for i2remote CLI tool
# Install with: pip install -r requirements.txt

# Core HTTP client
requests>=2.28.0

# Streaming uploads with progress for large files
requests-toolbelt>=1.0.0

# Azure AD authentication (optional, for 'login' command)
msal>=1.24.0
