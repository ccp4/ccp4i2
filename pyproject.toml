[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ccp4i2"
dynamic = ["version"]
description = "CCP4i2 graphical interface for CCP4 software"
readme = "README.md"
license = { file = "LICENSE" }
keywords = ["CCP4", "X-ray", "Crystallography", "MX", "Interface"]
classifiers = [
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Private :: Do Not Upload",
]
dependencies = [
    "biopython",
    "dials",
    "django",
    "dxtbx",
    "gemmi",
    "lxml",
    "matplotlib",
    "numpy",
    "onedep_api",
    "paramiko",
    "psutil",
    "PySide2",
    "pytest",
    "python-dateutil",
    "python-docx",
    "rdkit",
    "requests",
    "scipy",
    "shiboken2",
    "xia2",
]
requires-python = ">=3.9"

[project.scripts]
ccp4i2 = "ccp4i2.__main__:main"
i2archive = "ccp4i2.bin.i2ArchiveUtils:main"
i2buildCaches = "ccp4i2.bin.buildCaches:main"
i2defEd = "ccp4i2.bin.defEd:main"
i2exportProject = "ccp4i2.bin.exportI2Project:main"
i2exportAllProjects = "ccp4i2.utils.ExportAllProjects:main"
i2listProcesses = "ccp4i2.bin.listProcesses:main"
i2makeBoilerPlate = "ccp4i2.bin.makeBoilerPlate:main"
i2makeIss = "ccp4i2.bin.makeIss:main"
i2makeReport = "ccp4i2.bin.makeReport:main"
i2reconstructDB = "ccp4i2.bin.reconstructBrowser:main"
i2reconstructFromDBXML = "ccp4i2.bin.reconstructFromDBXML:main"
i2reconstructFromDirectoryList = "ccp4i2.bin.reconstructFromDirectoryList:main"
i2run = "ccp4i2.core.CCP4I2Runner:main"
i2runTask = "ccp4i2.bin.runTask:main"
i2testSys = "ccp4i2.bin.testi2sys:main"

[project.urls]
Source = "https://github.com/ccp4/ccp4i2"
Issues = "https://github.com/ccp4/ccp4i2/issues"

[tool.setuptools.dynamic]
version = { attr = "ccp4i2.__version__" }

[tool.setuptools.package-data]
ccp4i2 = ["*"]

# Non-PyPI dependencies in CCP4:
# - acedrg
# - adding_stats_to_mmcif
# - ample
# - ccp4mg
# - ccp4srs
# - clipper
# - iotbx
# - mmtbx
# - mrbump
# - mrparse
# - phaser
# - phaser_ext
# - pyrvapi
# - simbad

# Missing dependencies:
# - pycofe
